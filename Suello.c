Create a product structure that contanins the following information.
prodCode (int), prodName (String), prodDesc (String), prodCategory (PRODUCT_CATEGORY), prodQty (int), prodExp (Date)

Create a date structure that contains the foollowing inforamtion.
date (int), month (int), year (int)

Create a enum for PRODUCT_CATEGORY
FOOD, BEVERAGE

Create a enum for Boolean
FALSE, TRUE

Create the following function

Product newProduct() -> the function accpets all the product information and returns the information in a product.
Boolean stockIn() -> Accepts a product and a quantity, updates the current product's quantity adding the new quantity.UNICODEBool
Boolean hasStock() -> Checks a given product if stock is still available


